DailyFlow 0.6.0 重点改进了任务列表的分组、层级阅读和标题编辑体验。

- 任务分组现在可折叠，并持久保存每个分组的展开状态。
- Markdown 子任务支持缩进、层级引导线和后代折叠摘要，折叠时仍保留父任务上下文。
- 选中任务使用统一的 TickTick 风格表面，标题可直接行内编辑，并将光标正确定位到标题末尾。
- 任务详情重绘后会保留列表滚动位置，任务栏的导航、标题和底部操作不会随内容滚动。
- 完成专注、日历、任务详情和移动端功能的集成验证，并加强发布前的分支基线、功能合同和共享文件检查。
